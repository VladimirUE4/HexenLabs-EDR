# Frontend React

This is the React frontend for HexenLabs EDR.

## Setup

```bash
npm install
npm run dev
```

The frontend runs on `http://localhost:3000` and proxies API requests to `http://localhost:8080`.

## Build

```bash
npm run build
```

Output will be in `dist/` directory.

